interface:
  display_name: "Smoke Test"
  short_description: "Contract-driven browser smoke tests"
  default_prompt: "Use $smoke-test on this smoke test document. Validate that it has environment, account/auth, suite, and case coverage; recover and write back verified non-secret prerequisites if missing; then run the smoke cases and produce the report."
